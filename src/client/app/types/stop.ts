import {VehicleType} from './vehicle.type';

export class Stop {
  private cityId: string;
  private type: VehicleType;
  private name: string;
  private latitude: number;
  private longtitude: number;
  private parentStationId: string;
  private photoUrl: string;
}
